pkgtype(kind: "foreign_library")

// Runtime remains package-private. Only the scalar Wasm boundary functions are
// public so #export_name can expose them from this foreign-library artifact.
warnings = "-unused_value-unused_constructor"
